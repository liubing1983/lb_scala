package com.lb.scala.ch02

object Test {
  
  // |1 * 1 - 2| = 1
  // (1 + 2/1)/2 = 1.5
  
  def main(args : Array[String]) : Unit = {
    //sqrt(2)
  }
  
  //def sqrt(d : Double) : Double= {
    
    //val a = (a + d / a )/d;
    //println(a)
   // if(isabs(a, d))
   // a
    
   // else
     // sqrt(a);
 // }



    def isabs(a : Double, b : Double) =
      scala.math.abs(a * a - b) < 0.0001 ;
    
    
    
}